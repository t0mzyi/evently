<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Categories & Venues — Evently Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/assets/css/styles.css">
  <style>
    .category-thumb {
      font-size: 24px;
      /* Adjust size as needed */
      line-height: 1;
      display: inline-block;
      width: 24px;
      height: 24px;
      text-align: center;
    }
  </style>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            theme: {
              primary: '#1C2A3A',
              highlight: '#2C3E50',
              accent: '#ED8842',
              surface: '#F9F7F5'
            },
          },
          fontFamily: {
            heading: ['Fraunces', 'serif'],
            sans: ['Inter', 'sans-serif']
          }
        }
      }
    }
  </script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,300;400;500;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
</head>

<body class="bg-[#EAE0CF] text-[#1C2A3A] min-h-screen">

  <!-- Admin Navbar -->
  <%- include('../../partials/adminNavbar') %>
  <%- include('../../partials/doodles') %>
  <%- include('../../partials/toast') %>

  <main class="pt-32 pb-16 px-6 max-w-7xl mx-auto">
    <h1 class="text-4xl font-heading font-medium tracking-tight mb-2">Event Categories</h1>
    <p class="text-[#1C2A3A]/60 mb-12">Manage event categories for the platform</p>

    <div class="max-w-3xl mx-auto">
      <!-- Categories Section -->
      <div class="bg-white/60 backdrop-blur-xl rounded-[2rem] p-8 border border-white/40 shadow-sm">
        <div class="flex items-center justify-between mb-6">
          <h2 class="text-xl font-heading font-medium">Event Categories</h2>
          <a href="/admin/categories/add" class="px-4 py-2 bg-[#1C2A3A] text-white rounded-xl text-sm font-bold shadow-md hover:bg-[#2C3E50] transition-colors">
            + Add Category
          </a>
        </div>

        <div class="space-y-3">
          <% for(let i =0;i<categories.length ; i++) {%>
          <a href="/admin/category/<%= categories[i]._id %>" class="block">
            <div class="flex items-center justify-between p-4 rounded-xl bg-white/50 border border-[#1C2A3A]/5 hover:border-[#1C2A3A]/20 transition-colors group">
              <div class="flex items-center gap-3">
                <span class="category-thumb"><%= categories[i].iconUrl %></span>
                <div>
                  <p class="font-medium text-[#1C2A3A]"><%= categories[i].name %></p>
                  <p class="text-xs text-[#1C2A3A]/60"><%= categories[i].events.length %></p>
                </div>
              </div>
              <div class="flex items-center gap-2 opacity-0 group-hover:opacity-100 transition-opacity">
                <button onclick="event.preventDefault(); window.location.href='/admin/editCategory/<%= categories[i]._id %>" class="p-2 hover:bg-[#1C2A3A]/5 rounded-lg transition-colors">
                  <svg class="w-4 h-4 text-[#1C2A3A]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z">
                    </path>
                  </svg>
                </button>
              </div>
            </div>
          </a>
          <%}%>
        </div>
      </div>
    </div>
  </main>

</body>

</html>